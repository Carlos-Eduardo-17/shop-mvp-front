# URL base de la API del backend (incluye /api/ al final, tal como espera src/services/api.ts)
# Desarrollo local:
# VITE_API_URL=http://localhost:3000/api/

# Producción (reemplaza por la URL real de tu servicio en Render):
VITE_API_URL=https://tu-backend.onrender.com/api/
